# Hugging Face Spaces Environment Variables
# Copy this file to your HF Space Settings > Repository secrets

# Port (HF Spaces requires 7860)
PORT=7860

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS=https://masterpost-io.netlify.app,http://localhost:3000

# Optional: Model cache location (HF Spaces only allows /tmp)
U2NET_HOME=/tmp/.u2net
TORCH_HOME=/tmp/.torch
